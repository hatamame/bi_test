# **Python Streamlit BIツール仕様書 v2.0**

## **1\. 概要**

### **1.1. 目的**

本ツールは、プログラミングの専門知識がないユーザーでも、手持ちのCSVやExcelファイルを簡単にアップロードし、**簡単なデータ前処理を行った上で**データベースに格納し、**複数のグラフを組み合わせたダッシュボード**を作成・保存・共有できるインタラクティブなWebアプリケーションを開発することを目的とします。

### **1.2. ターゲットユーザー**

* データ分析を行いたいビジネス部門の担当者  
* 迅速にデータを可視化したいマーケターや営業担当者  
* Python/Streamlitの学習者

### **1.3. 主要機能**

* CSV/Excelファイルのアップロード機能  
* **データ前処理機能（データ型変更、欠損値処理）**  
* アップロードデータのデータベース（SQLite）への自動格納機能  
* **保存済みテーブルの管理機能（プレビュー、削除）**  
* **ダッシュボード作成機能（複数グラフのレイアウト）**  
* **ダッシュボードの保存・読み込み機能**  
* 対話的なグローバルフィルタリング機能  
* グラフ（棒、折れ線、散布図、円、ヒストグラム、**箱ひげ図、ヒートマップ**）によるデータ可視化機能  
* **データ・グラフのエクスポート機能（CSV, PNG）**

## **2\. 機能要件**

### **2.1. データ取り込み・管理機能**

* **ファイルアップロード:**  
  * ユーザーはWeb UIを通じて、.csv および .xlsx 形式のファイルをアップロードできる。  
* **テーブル名の設定:**  
  * ユーザーはアップロードするデータに対して、データベース内で使用する一意の「テーブル名」をテキスト入力で指定する。  
  * 指定がない場合は、ファイル名から拡張子を除いたものをデフォルトのテーブル名とする。  
* **【NEW】データ前処理 (オプション):**  
  * ファイルアップロード後、DB保存前の確認ステップとして設ける。  
  * **データプレビュー:** アップロードしたデータの先頭50行と各カラムの基本情報（列名、データ型、欠損値の数）を表示する。  
  * **データ型変換:** 各カラムのデータ型をドロップダウン（object, int64, float64, datetime64など）で変更できるようにする。  
  * **欠損値処理:** 欠損値を含むカラムに対して、処理方法（行ごと削除、0で埋める、平均値で埋める、中央値で埋める）を選択できるようにする。  
  * 変更内容はリアルタイムでプレビューに反映される。  
* **データベースへの格納:**  
  * **前処理が適用された**データを、指定されたテーブル名でSQLiteデータベースに永続的に保存する。  
  * もし同名のテーブルが既に存在する場合は、上書きするかどうかをユーザーに確認するダイアログを表示する。  
* **【NEW】保存済みテーブルの管理:**  
  * DBに保存されているテーブルの一覧を表示する。  
  * **プレビュー:** 各テーブルのスキーマ（列名、データ型）と先頭数行のデータをモーダルウィンドウで確認できる。  
  * **削除:** 不要なテーブルを選択してDBから削除できる。削除前には確認ダイアログを表示する。

### **2.2. データ可視化・分析機能 (ダッシュボード)**

* **【UPDATE】ダッシュボードレイアウト:**  
  * ユーザーはグリッドベースのレイアウト上に、複数の「ウィジェット（グラフや表）」を自由に追加・配置・リサイズできる。  
* **データソース選択:**  
  * ダッシュボードのベースとなるテーブルを、DBに保存されているテーブル一覧からドロップダウンで選択する。  
* **グローバルフィルター:**  
  * 画面のサイドバーなどにフィルター設定エリアを設ける。  
  * ここで設定したフィルターは、ダッシュボード上の**すべてのウィジェット**に一括で適用される。  
  * フィルターの種類はv1.0と同様（カテゴリ、数値、日付）。  
* **ウィジェット追加・設定:**  
  * 「ウィジェットを追加」ボタンを押すと、グラフ種別を選択するモーダルが表示される。  
  * グラフ種別は以下に拡充:  
    * 棒グラフ、折れ線グラフ、散布図、円グラフ、ヒストグラム  
    * **【NEW】箱ひげ図** (数値データの分布確認用)  
    * **【NEW】ヒートマップ** (カテゴリ間の相関確認用)  
  * グラフ種別を選択後、ウィジェットごとにX軸、Y軸、色分け、集計方法などを個別に設定する。  
  * **【NEW】KPIカード:** 特定の列の合計値、平均値、最大値などを大きな数字で表示するウィジェットも追加する。  
* **インタラクティブな操作:**  
  * グラフはPlotlyで描画し、ズーム、パン、ツールチップ表示などのインタラクティブな操作が可能。

### **2.3. 【NEW】共有・エクスポート機能**

* **ダッシュボードの保存・読み込み:**  
  * **保存:** 現在のウィジェットのレイアウト、各ウィジェットの設定、フィルターの状態をセットで「ダッシュボード」として名前を付けて保存できる。  
  * **読み込み:** 保存済みのダッシュボードを一覧から選択し、ワンクリックで状態を完全に復元できる。  
* **エクスポート:**  
  * **グラフのエクスポート:** 各ウィジェットの右上などにメニューを設け、表示されているグラフをPNG形式の画像ファイルとしてダウンロードできる。  
  * **データのエクスポート:** グローバルフィルター適用後のデータをCSVファイルとしてダウンロードできる。

## **3\. 非機能要件 (v1.0から変更なし)**

### **3.1. 使用技術スタック**

* **言語:** Python 3.9以上  
* **フレームワーク:** Streamlit (+ Streamlit-AgGrid, Streamlit-Dashboardなどのコンポーネントも検討)  
* **データ操作:** Pandas  
* **データベース:** SQLite  
* **グラフ描画:** Plotly Express

### **3.2. パフォーマンス**

* 10万行程度のデータ量であれば、ファイルのアップロードからグラフ描画まで、ストレスなく（数秒〜十数秒以内）動作することを目指す。

### **3.3. ユーザーインターフェース (UI)**

* Streamlitの標準ウィジェットを活用し、クリーンで直感的なUIを構築する。  
* 画面は大きく分けて「データ管理画面」と「分析ダッシュボード画面」の2つをサイドバーで切り替えられる構成とする。

## **4\. 画面設計（案） v2.0**

### **4.1. データ管理画面**

* **目的:** データの取り込みから前処理、DBへの保存、保存済みデータの管理までを一貫して行う。  
* **配置要素:**  
  * h1タイトル: 「データ管理」  
  * **セクション1: 新規データ取込**  
    * ファイルアップローダー → テーブル名入力 → **データ前処理・プレビューエリア** → DB保存ボタン  
  * **セクション2: 保存済みテーブル**  
    * テーブル名の一覧。各テーブルに「プレビュー」ボタンと「削除」ボタンを配置。

### **4.2. データ分析画面（ダッシュボード）**

* **目的:** データを自由に可視化し、インサイトを得る。作成したダッシュボードの保存・再利用を行う。  
* **レイアウト:**  
  * **ヘッダーエリア:**  
    * h1タイトル: 「データ分析ダッシュボード」  
    * ドロップダウン: 「分析するベーステーブルを選択」  
    * ドロップダウン: 「保存済みダッシュボードを読み込む」  
    * ボタン: 「現在のダッシュボードを保存」「ウィジェットを追加」「データをCSVエクスポート」  
  * **サイドバー（フィルターエリア）:**  
    * h3タイトル: 「グローバルフィルター」  
    * 各種フィルターコンポーネントを配置。  
  * **メインコンテンツエリア（ダッシュボード）:**  
    * グリッドレイアウト。  
    * ユーザーが追加したグラフ、KPIカード、データ表などのウィジェットが配置される。  
    * 各ウィジェットはドラッグ＆ドロップで移動・リサイズ可能。